# 기여 방법

1. 한국어 정본을 먼저 수정한다.
2. 교재 코드 블록을 복사하지 말고 `examples/` source를 수정한다.
3. `python -m pytest -q`와 Sphinx HTML build를 통과한다.
4. 실제로 실행하지 않은 ROS·hardware 결과를 PASS로 쓰지 않는다.
5. 제3자 이미지·코드에는 출처와 이용조건을 함께 제출한다.
