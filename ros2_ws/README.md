# ROS 2 workspace

이 디렉터리는 재사용 가능한 ROS 2 package가 실제 source inventory와 `colcon build/test` 증거를 갖춘 뒤 추가하는 workspace 경계다.

현재 private preview에는 독립 실행 예제만 `examples/`에 포함한다. 원본 34파일 package 전체가 공개 입력으로 확인되지 않았으므로 일부 파일을 추정해 조립하지 않는다.
