# 예제

예제 파일이 문서 코드 블록의 단일 진실원이다. 문서에서는 `literalinclude`로 이 파일을 포함한다.

현재 `navigate_to_pose_client.py`는 Python 문법·정적 구조 범위만 공개 후보에 포함한다. ROS 2 Humble package build, DDS, Gazebo·Nav2, 실물 Burger 통합은 HOLD다.
